settings.js
***********

**ПОЛЯ СУЩНОСТИ BaseSettings**

* **timezoneValues**
* **bufferSizeValues**
* **langValues**
* **timezone**
* **bufferSize**
* **lang**


**МЕТОДЫ СУЩНОСТИ BaseSettings**

* **saveSettings()**
* **loadSettings()**
* **clearSettings()**

Ниже представлен перечень get-теров и set-теров для пунктов экрана настроек. Ввиду того, что список этих пунктов различен для каждого шаблона, в BaseSettings указаны не все, а только основные. Соответственно, описание некоторых методов ниже также будет опущено по причине их тривиальности.

* **getTimezone()**
* **getTimezoneName()**
* **getTimezoneValue()**
* **getTimezoneValues()**
* **setTimezone(value)**
* **getDefaultTimezone()**
* **getBufferSize()**
* **getBufferSizeName()**
* **getBufferSizeValue()**
* **getBufferSizeValues()**
* **setBufferSize()**
* **getDefaultBufferSize()**
* **getLang()**
* **getLangName()**
* **getLangValue()**
* **getLangValues()**
* **setLang()**
* **getDefaultLang()**